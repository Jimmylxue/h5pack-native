!(function (e, t) {
  'object' == typeof exports && 'undefined' != typeof module
    ? t(exports)
    : 'function' == typeof define && define.amd
    ? define(['exports'], t)
    : t(
        ((e =
          'undefined' != typeof globalThis
            ? globalThis
            : e || self).H5PackBridge = {}),
      );
})(this, function (e) {
  'use strict';
  class t {
    h5packBridge;
    moduleName;
    constructor(e, t) {
      (this.h5packBridge = e), (this.moduleName = t);
    }
    async call(e, t) {
      return (
        console.log('this.moduleName', this.moduleName),
        this.h5packBridge.callNative(this.moduleName, e, t)
      );
    }
    handleError(e, t = 'Operation failed') {
      throw (
        (console.error(`[${this.moduleName}] Error:`, e),
        new Error(e.message || t))
      );
    }
  }
  const a = new (class {
    callbacks = new Map();
    modules = new Map();
    isAvailable = !1;
    constructor() {
      return (
        'undefined' != typeof window &&
          window.ReactNativeWebView &&
          (this.isAvailable = !0),
        this.setupEventListeners(),
        new Proxy(this, {
          get: (e, t) => (e.modules.has(t) ? e.modules.get(t) : e[t]),
        })
      );
    }
    registerModule(e, t) {
      this.modules.set(e, t);
    }
    callNative(e, t, a) {
      return new Promise((s, i) => {
        if (!this.isAvailable)
          return void i(new Error('Native bridge not available'));
        const r = `call_${Date.now()}_${Math.random()
            .toString(36)
            .substr(2, 9)}`,
          o = setTimeout(() => {
            this.cleanupCallback(r), i(new Error('Bridge call timeout'));
          }, 3e4);
        this.callbacks.set(r, {resolve: s, reject: i, timeoutId: o});
        const l = {
          type: 'bridge_call',
          callId: r,
          module: e,
          action: t,
          params: a,
          timestamp: Date.now(),
        };
        window.ReactNativeWebView.postMessage(JSON.stringify(l));
      });
    }
    setupEventListeners() {
      window.ReactNativeWebView &&
        document.addEventListener('message', this.handleNativeMessage),
        window.addEventListener('message', this.handleNativeMessage);
    }
    handleNativeMessage = e => {
      try {
        const t = JSON.parse(e.data);
        if ('bridge_response' === t.type) {
          const {callId: e, success: a, data: s, error: i} = t,
            r = this.callbacks.get(e);
          r &&
            (this.cleanupCallback(e),
            a ? r.resolve(s) : r.reject(new Error(i || 'Native call failed')));
        }
      } catch (e) {
        console.error('Failed to handle native message:', e);
      }
    };
    cleanupCallback(e) {
      const t = this.callbacks.get(e);
      t && (clearTimeout(t.timeoutId), this.callbacks.delete(e));
    }
  })();
  a.registerModule(
    'camera',
    new (class extends t {
      constructor(e) {
        super(e, 'camera');
      }
      async open(e) {
        try {
          return await this.call('open', {
            quality: e.quality || 'high',
            allowEdit: e.allowEdit || !1,
            maxWidth: e.maxWidth || 1920,
            maxHeight: e.maxHeight || 1080,
            cameraType: e.cameraType || 'back',
          });
        } catch (e) {
          return this.handleError(e, 'Failed to open camera');
        }
      }
      async checkPermission() {
        try {
          return await this.call('checkPermission');
        } catch (e) {
          return this.handleError(e, 'Failed to check camera permission');
        }
      }
      async requestPermission() {
        try {
          return await this.call('requestPermission');
        } catch (e) {
          return this.handleError(e, 'Failed to request camera permission');
        }
      }
    })(a),
  ),
    'undefined' != typeof window && (window.h5packBridge = a),
    (e.default = a),
    (e.h5packBridge = a),
    Object.defineProperty(e, '__esModule', {value: !0});
});
